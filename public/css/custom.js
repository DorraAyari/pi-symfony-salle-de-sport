$(document).ready(function() {
    // Code à exécuter une fois que le document est chargé

    // Exemple : Afficher une alerte quand on clique sur un bouton
    $('button').click(function() {
        alert('Le bouton a été cliqué !');
    });
});