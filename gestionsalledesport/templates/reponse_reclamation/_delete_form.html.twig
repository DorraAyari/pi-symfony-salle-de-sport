<form method="post" action="{{ path('app_reponse_reclamation_delete', {'id': reponse_reclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_reclamation.id) }}">
    <button class="btn">Delete</button>
</form>
